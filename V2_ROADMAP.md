# 第二版迭代路线图

## 目标

第二版不再以“把功能堆上去”为主，而是围绕下面三件事升级：

- 更顺手：搜索、批量操作、自动保存、编辑体验更自然。
- 更专业：3D 归档和知识库都补齐更像正式产品的能力。
- 更稳定：前后端结构拆分，降低后续继续开发的难度。

第一版已经完成了基础骨架：

- 模块化工作台
- 账号与模块权限
- 3D 文件归档
- 大数据知识库
- 系统管理
- Git + 服务器部署链路

第二版的重点，是把它从“能用”推进到“长期好用”。

## 必须做

这些内容建议作为 V2 第一阶段，优先级最高。

### 1. 知识库编辑体验升级

目标：

- 让大数据知识库真正适合长期写笔记、SQL、方案和复盘。

建议功能：

- Markdown 编辑/预览双模式
- SQL 代码高亮
- 常见代码块支持
- 自动保存
- 离开页面前未保存提醒
- 文档历史版本
- 更好的正文排版

完成标准：

- 文档内容可持续编辑，不再担心误操作丢数据。
- SQL、Markdown、普通正文的阅读体验明显提升。

### 2. 全局搜索升级

目标：

- 内容多起来之后，能快速找到文件、目录、文档和版本。

建议功能：

- 3D 模块支持按项目、目录、文件名、扩展名、更新时间搜索
- 知识库支持按标题、分类、正文、链接搜索
- 支持模块内筛选
- 支持最近更新排序

完成标准：

- 3D 文件和知识库内容都能在几步内被找到。

### 3. 批量操作体系化

目标：

- 让高频管理动作一次完成，不再一条条点。

建议功能：

- 批量移动
- 批量删除
- 批量下载
- 批量改权限
- 批量标记版本状态

完成标准：

- 3D 归档和知识库都具备稳定的批量管理能力。

### 4. 前后端结构拆分

目标：

- 避免第二版继续把逻辑都堆在一个文件里。

建议拆分：

- 前端把 [main.jsx](/Users/zmd/work/work-file-archive/frontend/src/main.jsx) 拆成 `archive`、`learning`、`admin`、`shared`
- 后端把 [main.py](/Users/zmd/work/work-file-archive/backend/app/main.py) 拆成 `auth`、`admin`、`archive`、`learning`
- 抽离 API 请求层
- 抽离权限判断和通用工具

完成标准：

- 后续新增功能时，不需要再长时间在单文件里来回找逻辑。

## 建议做

这些内容建议作为 V2 第二阶段，属于明显提升体验和专业度的升级。

### 1. 3D 归档专业化

建议功能：

- 文件备注和版本备注
- 项目状态流转：制作中、待确认、已交付、已归档
- 项目封面图或缩略图
- 自定义目录模板
- 更丰富的文件详情信息

### 2. 知识库内容增强

建议功能：

- 标签系统
- 资料附件
- 常用复盘模板
- 收藏/置顶
- 最近编辑文档

### 3. 审计与恢复能力

建议功能：

- 操作日志查询页
- 删除内容恢复入口更清晰
- 数据库备份和恢复操作界面化

### 4. 预览能力扩展

建议功能：

- `docx`、`xlsx` 文档预览
- 更多文本格式预览
- 条件允许时支持 `glb/gltf/obj/stl` 在线预览

## 后续做

这些方向适合放在 V2 后半段或者 V3，不建议一开始就做太重。

### 1. 更细粒度权限

- 项目级权限
- 目录级权限
- 文档级只读共享
- 模块管理员
- 用户组 / 角色组

### 2. 存储与传输升级

- 大文件断点续传
- 秒传/重复文件识别增强
- 对象存储接入

### 3. 数据库升级

- 从 SQLite 升级到 PostgreSQL
- 搜索索引优化
- 更完整的数据迁移脚本

数据库升级说明：

- 如果从 `SQLite` 升级到 `PostgreSQL`，通常需要额外准备数据库服务。
- 可以是你自己在服务器上部署 `PostgreSQL`，也可以使用云数据库。
- 升级后项目不再只是“应用服务”，而是“应用服务 + 独立数据库服务”两部分。

建议的升级时机：

- 用户数量明显增加
- 数据量越来越大
- 并发操作开始增多
- 需要更稳定的备份和恢复能力
- 需要更复杂的搜索、筛选和统计

如果当前还是以个人或少量用户使用为主，第二版前半段可以继续使用 `SQLite`，优先把编辑体验、搜索、批量操作和代码结构做好。

## 推荐开发顺序

建议按这个顺序推进：

1. 知识库编辑体验升级
2. 全局搜索升级
3. 批量操作体系化
4. 前后端结构拆分
5. 3D 归档专业化
6. 更细粒度权限

这个顺序的好处是：

- 先做用户最能直接感知的升级
- 再处理结构问题，降低后续开发成本
- 把复杂权限放到后面，避免一开始陷入重构泥潭

## 第二版里程碑

### V2.1

- 自动保存
- Markdown/SQL 编辑优化
- 搜索增强

### V2.2

- 批量操作
- 3D 项目信息补充
- 审计与恢复体验升级

### V2.3

- 前后端结构拆分
- 预览能力增强
- 权限细化准备

## V2.2 详细开发清单

下面这部分作为下一阶段的实际执行清单，优先级从高到低排列。

### P0：知识库编辑器稳定化

目标：

- 先把“大数据知识库”编辑体验收平，避免继续在布局和交互细节上反复返工。

建议任务：

- 阅读态与编辑态彻底分离，避免共用一套布局造成重叠。
- 左侧资料抽屉交互定型：默认收起、点击展开、拖动可调整位置。
- 标签选择器彻底统一：
  - 左侧维护标签池
  - 右侧只选择已有标签
  - 支持多选
  - 支持下拉弹出候选
- 历史快照默认收起，展开后不挤压正文输入区。
- SQL 编辑区高度、滚动、复制体验稳定化。

完成标准：

- 阅读态和编辑态切换后，页面结构不跳、不叠、不遮挡。
- 标签和历史快照不再引起布局异常。
- 正文编辑对长 SQL 和长笔记可稳定使用。

### P1：SQL 片段库补强

目标：

- 把知识库里的 SQL 能力从“能保存”提升到“能复用、能搜索、能整理”。

建议任务：

- 左侧 `SQL 片段库` 支持：
  - 搜索片段标题
  - 搜索 SQL 内容
  - 按标签筛选
- SQL 片段卡片增加结构化信息：
  - 名称
  - 用途
  - 来源表
  - 所属文档
- 支持一键复制 SQL。
- 支持一键跳回原文档。
- 支持从片段快速创建新文档或插入当前文档。

完成标准：

- 常用 SQL 不需要一条一条单独建文档。
- 用户可以通过片段库快速找到、复制和回看 SQL 来源。

### P2：知识库模板体系

目标：

- 让知识库从“白纸输入框”进化成“带工作流的知识沉淀工具”。

建议任务：

- 提供内置模板：
  - 学习笔记模板
  - SQL 模板
  - 复盘模板
  - 问题排查模板
- 新建文档时可直接选模板。
- 模板内容支持预填：
  - 标题结构
  - 标签建议
  - 正文骨架
- 后续支持自定义模板预留接口。

完成标准：

- 新建文档不再从空白开始。
- 同类内容结构更统一，便于后续复用和检索。

### P3：3D 归档专业化补充

目标：

- 让 3D 模块更像正式项目资产管理，而不只是文件树。

建议任务：

- 项目资料补充：
  - 负责人
  - 联系方式
  - 交付日期
  - 项目阶段
- 文件和版本补充：
  - 文件备注
  - 版本备注
  - 交付说明
  - 是否最终版标记
- 搜索补充：
  - 按项目状态筛选
  - 按更新时间筛选
  - 按上传人筛选
- 回收站补充：
  - 批量彻底删除确认
  - 恢复后定位原目录

完成标准：

- 项目信息不再只停留在名称和客户。
- 版本记录可以支持真实交付场景。

### P4：搜索入口统一

目标：

- 让 3D 归档和知识库都具备更统一的“查找”体验。

建议任务：

- 统一搜索输入交互和空状态。
- 搜索结果高亮命中内容。
- 增加最近搜索历史。
- 区分：
  - 当前目录搜索
  - 当前模块搜索
  - 全局搜索（后续可继续扩展）

完成标准：

- 用户能清楚知道当前搜的是哪里。
- 搜索结果更直观，不需要再反复点开确认。

### P5：系统管理增强

目标：

- 补足后台管理效率，而不是只停留在“能配权限”。

建议任务：

- 用户列表搜索
- 用户状态筛选
- 模块权限一键复制
- 用户授权模板
- 操作日志入口
- 模块启用/隐藏开关

完成标准：

- 管理员可以更快处理用户和模块授权。
- 后台不再依赖人工反复逐条配置。

## 推荐执行顺序

建议按下面顺序进入开发：

1. P0：知识库编辑器稳定化
2. P1：SQL 片段库补强
3. P2：知识库模板体系
4. P3：3D 归档专业化补充
5. P4：搜索入口统一
6. P5：系统管理增强

这样安排的原因是：

- 先把当前最常用、最容易引发体验问题的编辑器稳定下来。
- 再强化你最有价值的个人使用场景：SQL 沉淀和知识复用。
- 最后再补 3D 管理、全局搜索和后台效率。

## 结论

第二版最值得追求的，不是单纯增加功能数量，而是把这套系统做成：

- 真正适合日常长期使用
- 数据越来越多时依然好找、好管、好维护
- 后面继续迭代时不会越来越难改
