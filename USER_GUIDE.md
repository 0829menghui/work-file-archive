# 使用文档

本文档只对应 `work-file-archive` 项目。

线上访问地址：

```text
http://124.223.78.223/
```

默认管理员账号：

```text
用户名：admin
密码：admin123
```

上线后建议尽快修改管理员密码。

## 模块说明

当前系统包含三个入口：

- `3D 文档归档`: 保存 3D 工程文件、贴图、模型、渲染图、交付文件等。
- `大数据知识库`: 写学习笔记、SQL 模板、方案和复盘文档。
- `系统管理`: 管理用户和模块权限。

不同用户登录后，只会看到自己有权限访问的模块。

## 3D 文档归档

### 项目

左侧可以选择项目，也可以新建项目。

每个项目下都有独立目录树，适合按客户、项目或交付周期存放 3D 文件。

### 目录

支持：

- 新建目录
- 多级目录
- 拖拽移动目录
- 拖拽移动文件
- 把子目录拖回一级目录

### 上传

支持两种上传方式：

- 上传文件
- 上传文件夹

上传文件夹时，会保留本地文件夹里的子目录结构。

### 版本

如果同一个目录下再次上传同名文件，系统不会覆盖旧文件，而是生成新版本。

文件详情中可以查看历史版本，也可以把旧版本标记为失效。

### 下载

支持：

- 下载单个文件
- 下载某个历史版本
- 打包下载当前目录

### 预览

第一版支持常见文件在线预览：

- 图片
- PDF
- 视频
- 文本
- Markdown
- SQL
- JSON
- CSV

3D 软件源文件暂时以归档、下载、版本管理为主。

## 大数据知识库

### 知识目录

左侧是知识目录，可以建立多级文件夹。

文件夹用于分类存放文档，文档可以拖进文件夹，也可以拖回一级目录。

### 文档

支持：

- 新建文档
- 编辑标题
- 编辑分类
- 设置状态
- 设置优先级
- 记录资料链接
- 编写正文
- 删除文档
- 批量删除

文档默认是阅读状态，点击 `编辑` 后才进入编辑状态。

保存后会回到阅读状态。

### SQL 内容

如果正文是 SQL，阅读状态会按 SQL 代码块展示。

编辑状态下可以点击 `格式化 SQL`，把 SQL 关键字大写并做简单换行。

## 系统管理

管理员可以：

- 修改模块名称、说明和排序
- 创建用户
- 创建管理员
- 给用户分配模块权限
- 修改已创建用户对每个模块的权限
- 修改用户显示名、用户名、角色和账号状态
- 重置用户密码
- 删除用户账号

管理员默认拥有全部模块管理权限。如果需要限制某个账号只能看指定模块，请先把该账号角色改为普通用户，再在 `用户模块权限` 里单独授权。

模块权限包括：

- `无权限`: 看不到模块。
- `仅查看`: 可以查看，不能新增、上传、编辑、删除。
- `可编辑`: 可以使用日常编辑能力。
- `管理`: 管理员级权限。

## 日常更新

本项目的上传和部署文档在：

```text
GIT_DEPLOY.md
```

本地发布命令：

```bash
cd /Users/zmd/work/work-file-archive
bash upload_deploy_restart.sh "更新说明"
```

不要把本项目文档放到 `requirement-archive` 目录里。
